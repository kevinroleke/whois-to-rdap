# Whois -> RDAP Proxy

Simple whois server that will return RDAP query for the domain. Intentionally does not work with IP addresses, ASNs, and other entities other than domain names (check domain functionality in main.go).
